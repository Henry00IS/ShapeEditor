#if UNITY_EDITOR

namespace AeternumGames.ShapeEditor
{
    /// <summary>The shape select mode (e.g. vertex or edge or face select).</summary>
    public enum ShapeSelectMode
    {
        Vertex,
        Edge,
        Face
    }
}

#endif